export type contactType = {
    firstSection: {
        hidden: boolean;
        pageTitle: string,
        mainTitle: string,
        subTitle: string
    },
    secondSection: {
        hidden: boolean;
        mainTitle: string,
        subTitle: string,
        addressTitle: string,
        location: string,
        telephone: string,
        email: string,
        fax: string,
        timings: string,
        map: string
        getDirection: string
    }
}
